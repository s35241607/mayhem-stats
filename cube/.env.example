# 複製成 .env 即可使用。這裡沒有真正的機密——Cube 只讀本機的 SQLite 檔,
# 而且前端是透過 FastAPI 的 /api/cube 代理存取,不直接連 Cube 的埠。
CUBEJS_DEV_MODE=true
CUBEJS_DB_TYPE=sqlite
CUBEJS_DB_NAME=../mayhem.db
CUBEJS_API_SECRET=mayhem-local-dev-secret
CUBEJS_LOG_LEVEL=info
PORT=4000
